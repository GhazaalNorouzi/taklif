/** @format */
//add comments
// explain what this function does. you can use Google!

function whatDoIDo() {
  return Math.random() * 8 + 1;
}
console.log(whatDoIDo());
// answer:
// Math.random() produces a number between 0 and 1
// Multiplying by 8 gives a range of 0 to 8
// Adding 1 turnd the range to 1 to 9
// So the function returns a random number between 1 and 9
//

// explain what does this function do, google it
function whatICanDo(word1, word2) {
  return word1.concat(word2);
}

// answer:
// this function combines two strings together
